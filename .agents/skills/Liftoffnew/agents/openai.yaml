interface:
  display_name: "Liftoffnew"
  short_description: "Repo-specific patterns and workflows for Liftoffnew"
  default_prompt: "Use the Liftoffnew repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true